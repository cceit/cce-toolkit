from distutils.core import setup
setup(
    name = 'cce_toolkit',
    packages = ['cce_toolkit'], # this must be the same as the name above
    version = '0.1',
    description = 'Utils for CCE-IT developers (joint authorship)',
    author = 'Alaina Rowe',
    author_email = 'atroiano@ou.edu',
    # url = 'https://github.com/peterldowns/mypackage', # use the URL to the github repo
    # download_url = 'https://github.com/peterldowns/mypackage/tarball/0.1', # I'll explain this in a second
    keywords = ['CCE-IT', 'tools', 'utils'],
    classifiers = [],
)

